#!/usr/bin/env bash

echo "Loading arguments"

while test $# -gt 0
do
  declare argument=$1

  case argument in
    -l|--letterbox)
      LETTERBOX="$2"
      shift # past key
      shift # past value
    ;;
  esac

  echo Variable  $argument
  echo Letterbox $LETTERBOX
done
