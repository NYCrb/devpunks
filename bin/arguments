#!/usr/bin/env bash

echo "Loading arguments"

while test $# -gt 0
do
  case $1 in
    -l|--letterbox)
      declare -r $LETTERBOX=$2
      shift # past key
      shift # past value
    ;;
  esac

  echo Variable $1
  echo Letterbox $LETTERBOX
done
