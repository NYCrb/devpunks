#!/usr/bin/env bash

# SSIM (Structural Similarity Metric) https://ffmpeg.org/ffmpeg-filters.html#ssim

echo Loading stats

declare -r  REPORT="-report"
declare -r  REFERENCE="movie=FEAR.mp4"
declare -r  PTS="setpts=PTS-STARTPTS"
declare -r  SSIM="stats_file=stats.log"
declare -r  STATS="-stats $REPORT $REFERENCE,$PTS $SSIM"
