#!/usr/bin/env bash

echo Loading letterbox

function letterbox.color {
  local color="black"
  local filter="pad=720:720:(ow-iw)/2:(oh-ih)/2:$color"
  echo $filter
}

function letterbox.blur {
  echo Blurred
}
